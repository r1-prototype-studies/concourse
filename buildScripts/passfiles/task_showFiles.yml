---
platform: linux

image-resource:
  type: docker-image
  source: { repository: busybox }

inputs:
  -name: repo
  -name: infolder

run:
  path: /bin/sh
  args: [repo/buildScripts/passfiles/showFiles.sh]
